services:
  api:
    environment:
      ANVA_FILESYSTEM_ALLOWED_ROOTS: /fixtures
    volumes:
      - ../anva-test:/fixtures/anva-test:ro

  worker:
    environment:
      ANVA_FILESYSTEM_ALLOWED_ROOTS: /fixtures
    volumes:
      - ../anva-test:/fixtures/anva-test:ro

  cli:
    environment:
      ANVA_FILESYSTEM_ALLOWED_ROOTS: /fixtures
    volumes:
      - ../anva-test:/fixtures/anva-test:ro

  test:
    environment:
      ANVA_FILESYSTEM_ALLOWED_ROOTS: /fixtures
      ANVA_TEST_CORPUS_ROOT: /fixtures/anva-test
    volumes:
      - ../anva-test:/fixtures/anva-test:ro
