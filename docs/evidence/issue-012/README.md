# Issue 012 browser evidence

Generated by the isolated Compose browser test:

```bash
docker compose -p anva-i12-impl --profile test --profile browser run --rm \
  -e ANVA_PERFORMANCE_COMMIT=e157a3f34aea2730fe5766d1cc79124440673b6d \
  -e ANVA_PERFORMANCE_IMAGE=anva-i12-impl-browser-test@sha256:d840be65a8362767 \
  browser-test \
  pytest -q tests/browser/test_canvas_browser.py --maxfail=1
```

Environment: Selenium 4.34.2, Chromium and ChromeDriver 150.0.7871.181,
1440×1024 desktop, 320×760 mobile with page JavaScript disabled, and a 200%
browser zoom check. Records are synthetic tenant fixtures. The source-backed
relationship is created by the real filesystem ingestion boundary inside the
isolated browser-test database. Images contain no credential, cookie, token,
environment value, or customer source payload.

| Image | Evidence | SHA-256 |
| --- | --- | --- |
| `01-canvas-desktop.png` | Source-backed 74-node/167-relationship semantic map, typed controls, minimap, and inspector | `3e01dc1687e49dcd6d6b972a2cdeea73173f84443fd5cf9ac7bd4288ef825914` |
| `02-canvas-inspector-saved-layout.png` | Populated node inspector, drag, zoom, annotation, and immutable saved layout revision 2 | `5f1070cd579d2ed1d8bb71ca8f7f83adce5b83d384e4019b2eab3bb325e318e2` |
| `03-canvas-mobile-320-no-js.png` | JavaScript-disabled canonical node table at 320 pixels | `0d24b5ee393aec48bcec991c71cb15aa1e23b516ca55180fbff3637397c72dd4` |
| `04-canvas-browser-zoom-200.png` | Readable, non-overflowing 300-node Canvas at 200% browser zoom | `847a9fea9d7074deb696e2bb7af332dc228edfede418dc5b564fb30316245330` |
| `05-canvas-300-node-performance.png` | Standard-budget 300-node/167-relationship map rendered and fit | `cc490f4799f39c96a0036ba64f489923ed9b70407959d7afe8adef96eb54372e` |
| `06-canvas-required-traces.png` | Visible product → repository → team explanation; the same journey also proves the goal → initiative → requirement → pull-request trace | `2472a27ff3df07d355d33508ae747dc4b9075596304ba913f2fb4d931e338454` |
| `07-canvas-dense-progressive-focus.png` | Dense graph progressively focused to one hop around the service: 42 nodes and 114 relationships | `cd39982faf454ba995cec53c7e29bb47d0cdaa0728baafc85cf2d5da4de37bcf` |

The test additionally asserts the dense fixture has a directed cycle, parallel
typed edges, maximum degree 42, stale and inferred nodes, a conflict, and no
hidden-canary leak. Dagre is available locally, every edge-disconnected node is
rendered in a deterministic compact grid outside the connected layout, saved
pins win for connected and isolated nodes, and all 300 positions remain
identical across 31 renders. Exact local search still reaches disconnected
nodes. The minimap is visible, all 24 proposal relationship types are present,
selection updates the inspector, save reaches revision 2, and relationship
submission creates a proposal without changing canonical relationships. The
no-JS page contains no graph nodes. Every capture asserts document/body width
and the actual shell, main, sidebar, title, controls, and focus-control bounds;
the responsive states have no document-level horizontal overflow. The browser
console contains no unexpected severe entry.

The raw, post-warm 30-sample measurements are committed as
`performance/browser.json` and `performance/database.json`. Both reports bind
to source/test commit `e157a3f34aea2730fe5766d1cc79124440673b6d`. On the
recorded browser run, the 300-node/167-relationship shell-to-interactive
p50/p95/max was 651.3/792.5/1,112.7 ms, connected-node Dagre layout was
155.65/202.1/286.4 ms, and the HTTP path was 159.65/236.2/244.7 ms. Local
search/filter/select p95 was 15.9/10.9/0.2 ms. Canvas gesture work was
0.5/1.3/2.1 ms; the event-to-rAF diagnostic was 16.55/48.1/48.5 ms and is not
represented as physical display timing. The gesture measurement window
observed no long task, all 30 samples made zero external request, and the graph
payload was 231,445 bytes.

On the recorded database run, strict authorization/query/hydration p95 was
160.897 ms with exactly 21 queries in every sample. Path service p95 was
127.456 ms with exactly 18 queries in every sample. Its 300-node source-backed
payload was 139,284 bytes. These remain below the 400 ms strict-query, 1,000 ms
path, and 768,000-byte payload gates. Query-count equality is the explicit N+1
regression assertion.

Vendored artifact review:

| Artifact | SHA-256 |
| --- | --- |
| `dagre.min.js` | `e073937ba0b6918fd3bba7d50a61d525b18e9dabf6ed8b208abbc0eed11be1ee` |
| `LICENSE` | `6a349742a6cb219d5a2fc8d0844f6d89a6efc62e20c664450d884fc7ff2d6015` |
| `LEGAL.txt` | `9148bffb1e84382a8b6668eeb2b53c6a554341d714fba129856ea5eb350d35f3` |

The vendored release JavaScript is unchanged except that its optional
`sourceMappingURL` trailer is removed. This keeps the production WhiteNoise
manifest closed without shipping a 201,454-byte development-only source map.

The exact source/test browser run passed in 90.20 seconds and the exact database
run passed in 38.05 seconds. During evidence
generation, only isolated PostgreSQL and MinIO support services remained
running (approximately 260 MiB combined). The one-off browser image is
1,260,998,368 bytes and includes the matching Debian Chromium/ChromeDriver and
Selenium versions.
