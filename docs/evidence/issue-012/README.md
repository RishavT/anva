# Issue 012 browser evidence

Generated by the isolated Compose browser test:

```bash
docker compose -p anva-i12-toctou-audit --profile test --profile browser run --rm \
  -e ANVA_PERFORMANCE_COMMIT=b677585542e1a9a24c913274112a59ef7a106dce \
  -e ANVA_PERFORMANCE_IMAGE=anva-i12-toctou-audit-browser-test@sha256:6aadad02ad7d708636d783803ca986bbb8f94ad438c7ff6c788bc272f255dee8 \
  browser-test \
  pytest -q \
  tests/browser/test_canvas_browser.py::test_organizational_canvas_interaction_no_js_and_responsive_evidence
```

Environment: Selenium 4.34.2, Chromium and ChromeDriver 151.0.7922.71,
1440×1024 desktop, 320×760 mobile with page JavaScript disabled, and a 200%
browser zoom check. Records are synthetic tenant fixtures. The source-backed
relationship is created by the real filesystem ingestion boundary inside the
isolated browser-test database. Images contain no credential, cookie, token,
environment value, or customer source payload.

| Image | Evidence | SHA-256 |
| --- | --- | --- |
| `01-canvas-desktop.png` | Source-backed 74-node/167-relationship semantic map, typed controls, minimap, and inspector | `f28caf06a555850b2f10c516e18d5e84ed415d9852161c2c616ca738cf814b2c` |
| `02-canvas-inspector-saved-layout.png` | Populated node inspector with reviewers/conflicts, drag, zoom, annotation, and immutable saved layout revision 2 | `e7aa5c857f7ede3052200d25039893912634bf028d9eb66d7cb3ebf3b820bb1e` |
| `03-canvas-mobile-320-no-js.png` | JavaScript-disabled canonical node table at 320 pixels | `f23d75e31e4076b1167595eca26456baf39e094bb934aad7ccb48c34558f1e80` |
| `04-canvas-browser-zoom-200.png` | Readable, non-overflowing 300-node Canvas at 200% browser zoom | `cafb544a7b0994f53444c95db09ebee43b855c3df1b1f9d2f801d576a517f288` |
| `05-canvas-300-node-performance.png` | Standard-budget 300-node/167-relationship map rendered and fit | `b8156ee71e777776aed14093e226996e5c890af1ed6b4fcd79004eaf5dae9632` |
| `06-canvas-required-traces.png` | Visible product → repository → team explanation; the same journey also proves the goal → initiative → requirement → pull-request trace | `af676271b7c35717ff88142135198ed66bee59fed7b9a4a78cd3b02a008bee5d` |
| `07-canvas-dense-progressive-focus.png` | Dense graph progressively focused to one hop around the service: 42 nodes and 114 relationships | `73897e169b8b6c26b42838aa12e3d8b8f8dbee158b243a38f77788cbe7f9deaa` |

The test additionally asserts the dense fixture has a directed cycle, parallel
typed edges, maximum degree 42, stale and inferred nodes, a conflict, and no
hidden-canary leak. Dagre is available locally, every edge-disconnected node is
rendered in a deterministic compact grid outside the connected layout, saved
pins win for connected and isolated nodes, and all 300 positions remain
identical across 31 renders. Exact local search still reaches disconnected
nodes. The minimap is visible, all 24 proposal relationship types are present,
selection updates the inspector, reverse-ordered detail responses cannot replace
the newer selection, and a current request failure clears every previously
loaded inspector field and bounded list. Save reaches revision 2, and relationship
submission creates a proposal without changing canonical relationships. The
no-JS page contains no graph nodes. Every capture asserts document/body width
and the actual shell, main, sidebar, title, controls, and focus-control bounds;
the responsive states have no document-level horizontal overflow. The browser
console contains no unexpected severe entry.

The raw, post-warm 30-sample measurements are committed as
`performance/browser.json` and `performance/database.json`. Both reports bind
to source/test commit `b677585542e1a9a24c913274112a59ef7a106dce`. Before
either report was written, its nested metric summaries were recursively
recomputed from the exact rounded values serialized in each `raw` array. On the
recorded browser run, the 300-node/167-relationship shell-to-interactive
p50/p95/max was 602.25/821.6/890.4 ms, connected-node Dagre layout was
165.8/195.2/211.5 ms, and the HTTP path was 166.0/195.2/195.4 ms. Local
search/filter/select p95 was 13.6/13.0/0.8 ms. Canvas gesture work was
0.55/1.4/1.5 ms; the event-to-rAF diagnostic was 16.6/34.7/36.8 ms and is not
represented as physical display timing. The gesture measurement window
observed no long task, all 30 samples made zero external request, and the graph
payload was 231,440 bytes.

On the recorded database run, strict authorization/query/hydration p95 was
211.028 ms with exactly 21 queries in every sample. Path service p95 was
134.783 ms with exactly 18 queries in every sample. Its 300-node source-backed
payload was 139,284 bytes. These remain below the 400 ms strict-query, 1,000 ms
path, and 768,000-byte payload gates. Query-count equality is the explicit N+1
regression assertion. The separate 100-repository × 100-scope detail batch
completed in 0.449443 seconds with 20 statements; its slowest statement was
0.104 seconds. The saved-view integration regressions separately prove that one
and 300 visible views both use exactly six statements; grant, credential, and
scope-principal revocations committed after scope resolution are rejected by
the one authoritative candidate statement; and a 3,902-row fixture with 3,300
authorized views plus 301 earlier and 301 later inaccessible views stops after
at most 601 ordered index rows. PostgreSQL uses `core_canvas_view_list_idx`
without a `Sort` or materialized CTE scan.

Vendored artifact review:

| Artifact | SHA-256 |
| --- | --- |
| `dagre.min.js` | `e073937ba0b6918fd3bba7d50a61d525b18e9dabf6ed8b208abbc0eed11be1ee` |
| `LICENSE` | `6a349742a6cb219d5a2fc8d0844f6d89a6efc62e20c664450d884fc7ff2d6015` |
| `LEGAL.txt` | `9148bffb1e84382a8b6668eeb2b53c6a554341d714fba129856ea5eb350d35f3` |

The vendored release JavaScript is unchanged except that its optional
`sourceMappingURL` trailer is removed. This keeps the production WhiteNoise
manifest closed without shipping a 201,454-byte development-only source map.

The exact source/test browser run passed in 88.66 seconds and the exact database
run passed in 72.53 seconds. During evidence generation, only isolated
PostgreSQL and MinIO support services remained running (approximately 290 MiB
combined). The one-off browser image is 1,317,444,559 bytes and includes the
matching Debian Chromium/ChromeDriver and Selenium versions.
