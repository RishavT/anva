# Anva 0.1.5

Status: **technically published and verified; human operator acceptance #44 remains open**.

The public [`v0.1.5` release](https://github.com/RishavT/anva/releases/tag/v0.1.5)
binds to product source `491cdd7830a7f4d6af7140f6a4744f95c80c46a9` and immutable image
`ghcr.io/rishavt/anva@sha256:19488230c6f7900cda33bd11adc7f1ad824d23b77ee87fd65ac883cd0dacc725`.
Release workflow [33727525411](https://github.com/RishavT/anva/actions/runs/33727525411)
successfully built, scanned, approved, attested, published, downloaded, and
verified the immutable install lifecycle.

This fix-forward candidate includes deterministic construction from #91,
single-byte OCI lineage from #96, protected decision verification from #101,
and the least-privilege Skopeo auth-file identity fix from #106 in one immutable
product source. The release workflow accepts only `v0.1.5` and requires the
full candidate commit. The tag, checkout, OCI revision, artifacts, predicates,
and published digest must all bind to that exact commit.

Publication included a fresh attested vulnerability proposal and explicit
personal protected-environment decision by RishavT. That release risk decision
does not perform or approve the separate human operator acceptance drill.
The v0.1.0 vulnerability exception is historical evidence and was not replayed
as authorization for v0.1.5.

Rollback uses the verified immutable v0.1.0 digest only after confirming schema
compatibility, or restores a verified backup. Tags, assets, attestations, and
digests are never moved or overwritten.
