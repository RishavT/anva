# Anva 0.1.1 — aborted, unpublished

Status: aborted and unpublished. The immutable `v0.1.1` tag remains bound to
`d813c9b75923285761cfc3ec1105e63ca98aea0e`, but no v0.1.1 GHCR image or GitHub
Release was published. Same-source proposal runs produced different image digests;
runs `33691370693` and `33698109859` were cancelled with zero approvals.

This candidate source combined the supported decommission cleanup retry from #73
and the synthetic operator drill harness from #80. Its release workflow required
the full candidate commit and exact tag/source binding, but no candidate passed
the reproducible-image boundary and publication was never authorized.

The #80 harness verifies the automation surface and records whether the
decommission CLI is present in that exact product image. It does not perform,
approve, or finalize the separate human #44 operator acceptance drill.

The deterministic image fix from #91 landed after this immutable tag, so a
workflow dispatched from fixed main still checked out the older nondeterministic
build context. The tag will not be moved or recreated. Release work fixes forward
through the aborted v0.1.2 through v0.1.4 attempts and now to v0.1.5, whose future immutable
source includes the fixed build machinery and #96 OCI lineage. The v0.1.0 vulnerability exception is historical evidence and
was never valid for v0.1.1.

Rollback means redeploying the previously verified immutable v0.1.0 digest only
after confirming schema compatibility or restoring a verified backup. Tags,
release assets, attestations, and image digests are never moved or overwritten.
