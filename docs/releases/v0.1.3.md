# Anva 0.1.3 — aborted, unpublished

Status: aborted and unpublished. The immutable `v0.1.3` tag remains bound to
`ae6310a942b96ca0173d66cd452b09ec218b0118`. No v0.1.3 GHCR image or GitHub
Release was published.

Release run `33713418248` proposed candidate image digest
`sha256:135993d74ccc27a1f286b56eb92cbd66e6f93b9119108105db60cb95bbf34e73`.
It received exactly one RishavT `Build and attest` environment approval. The
protected build verified that approval and the proposal, rebuilt the exact
candidate, generated the risk decision, and created and uploaded its
attestation. The following decision-attestation readback step failed closed
because its image-digest environment binding was missing. The run received zero
Publish or Verify approvals, and the publishing, release, and installation
verification jobs did not run.

The decision attestation and failed run are immutable historical evidence, not
a published release and not authorization for another candidate. #101 fixes
the missing verification binding after the immutable v0.1.3 source. Release
work continued through the aborted v0.1.4 attempt and fixes forward to v0.1.5.
Historical v0.1.0 repair evidence and the aborted v0.1.1 through v0.1.4 attempts
cannot authorize v0.1.5. The
separate human operator acceptance drill is not approved by release automation.

Rollback uses the verified immutable v0.1.0 digest only after confirming schema
compatibility, or restores a verified backup. Tags, assets, attestations, and
digests are never moved or overwritten.
