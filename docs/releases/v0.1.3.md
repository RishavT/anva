# Anva 0.1.3

Status: release candidate preparation. No `v0.1.3` tag, image, approval, or
GitHub Release exists until separately authorized release steps succeed.

This fix-forward candidate includes the supported decommission cleanup retry,
synthetic operator drill harness, deterministic construction, hardened release
evidence, and #96 single-byte OCI lineage in one immutable product source. The
release workflow accepts only `v0.1.3` and requires the full candidate commit.
The tag, checkout, archive, OCI manifest, artifacts, predicates, decision, and
published digest must all bind to that exact commit.

Publication remains gated by a fresh attested vulnerability proposal and an
explicit personal protected-environment decision by RishavT. The v0.1.0
vulnerability exception is historical evidence, and it and the aborted,
unpublished v0.1.1 and v0.1.2 attempts cannot be
replayed for v0.1.3. The separate human operator acceptance drill is not
approved by release automation.

Rollback uses the verified immutable v0.1.0 digest only after confirming schema
compatibility, or restores a verified backup. Tags, assets, attestations, and
digests are never moved or overwritten.
