# Anva 0.1.4 — aborted, unpublished

Status: aborted and unpublished. The immutable `v0.1.4` tag remains bound to
`098e7727d4c307a8bbf25c05c36b0d27e25b4274`. No v0.1.4 GHCR image or GitHub
Release was published.

Release run `33718942806` proposed and reproduced candidate image digest
`sha256:c6ac01350fa42ff915827d0ca1c130e89881e69a11c59cb54c00262377a17169`.
It received exactly one RishavT `Build and attest` environment approval whose
comment explicitly excluded Publish and Verify. The protected build verified
that approval and the proposal, reproduced the exact candidate, generated and
attested the risk decision, and passed its immediate attestation readback. The
first Skopeo registry-copy command then failed closed because the dropped-
capability container could not read the runner-owned mode-0600 auth bind.

Run `33718942806` received zero Publish or Verify approvals. It published no
GHCR image or GitHub Release, and its later release and verification jobs did
not run. The decision attestation and failed run are immutable historical
evidence, not authorization for another candidate. #106 fixes the auth-file
identity after the immutable v0.1.4 source, so release work fixes forward to
v0.1.5. No v0.1.4 approval or decision authorizes v0.1.5.

Rollback uses the verified immutable v0.1.0 digest only after confirming schema
compatibility, or restores a verified backup. Tags, assets, attestations, and
digests are never moved or overwritten.
