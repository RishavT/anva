# Anva 0.1.6

Status: release preparation. No `v0.1.6` tag, image, approval, or GitHub Release
is claimed until the protected release workflow succeeds.

This candidate carries the generic operator-evidence source-role contract from
#117/#118. Evidence creation records the current harness source separately from
the immutable product/operator-tool source and verifies the latter against the
selected image's OCI revision label. The actual product commit, image digest,
release run, and eligibility must be supplied and verified at runtime; this
document does not invent those future identities.

The successful v0.1.5 release remains immutable historical evidence at product
source `491cdd7830a7f4d6af7140f6a4744f95c80c46a9` and image digest
`sha256:19488230c6f7900cda33bd11adc7f1ad824d23b77ee87fd65ac883cd0dacc725`.
Its operator acceptance remains `NOT_ACCEPTED` because its drill tool predates
the source-role contract. The v0.1.0 vulnerability exception is historical
evidence, and no earlier risk approval authorizes v0.1.6.

Publication requires a fresh exact-candidate scan, attested risk proposal,
explicit RishavT protected-environment decision, immutable artifacts and
attestations, and post-publication install verification. The separate #44 human
operator exercise may begin only after the exact published v0.1.6 digest proves
an eligible evidence header; automation must not create its decisions or
signoff.
