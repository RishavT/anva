# Anva 0.1.2 — aborted, unpublished

Status: aborted and unpublished. The immutable `v0.1.2` tag remains bound to
`53f58e6bc01b0b5ac8316f030ba17048285049b6`, but no v0.1.2 GHCR image or
GitHub Release was published.

Release run `33703772407` proposed candidate digest
`sha256:caf1e15ade5c65fe507c6ff019064b0c4165904e760535cf3c690033d7a0acd9`.
Its protected rebuild produced
`sha256:cfc7dfaf74c28f18f3fb0fc29eb09c6d7a5b23f7b88183c70deec7af79fafc49`
and failed the exact-candidate binding before publication. The run received
exactly one RishavT `Build and attest` environment approval. It produced no
generated or attested risk decision, received no publish/verify approval, and
published no GHCR image or GitHub Release. The tag is not moved or recreated.

This source includes deterministic construction from #91 but predates #96's
single-byte OCI transfer design. Release work continued through the aborted
v0.1.3 attempt and now fixes forward to v0.1.4, whose
future immutable source contains both product code and the corrected release
lineage. Historical v0.1.0 evidence and the aborted v0.1.1/v0.1.2 evidence
cannot authorize v0.1.4. The separate human operator acceptance drill is not
approved by release automation.

Rollback uses the verified immutable v0.1.0 digest only after confirming schema
compatibility, or restores a verified backup. Tags, assets, attestations, and
digests are never moved or overwritten.
