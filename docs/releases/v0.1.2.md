# Anva 0.1.2

Status: release candidate preparation. No `v0.1.2` tag, image, or GitHub
Release exists until a separately authorized protected-environment run succeeds.

This fix-forward patch includes the supported decommission cleanup retry, the
synthetic operator drill harness, hardened release evidence, and deterministic
container construction from #91 in one immutable product source. The release
dispatch accepts only `v0.1.2` and requires the full candidate commit. The tag,
checkout, OCI revision, artifacts, predicates, and published digest must all bind
to that exact commit.

Publication remains gated by a fresh attested vulnerability proposal and an
explicit personal protected-environment decision by RishavT. The v0.1.0
vulnerability exception is historical evidence; it and the aborted v0.1.1
proposals cannot be replayed for v0.1.2.
The separate human operator acceptance drill is not approved by release automation.

Rollback uses the verified immutable v0.1.0 digest only after confirming schema
compatibility, or restores a verified backup. Tags, assets, attestations, and
digests are never moved or overwritten.
